<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <!-- NuGet Package Information -->
    <PackageId>MasLazu.AspNet.Authentication.Password.EfCore</PackageId>
    <Version>1.0.0-preview.1</Version>
    <Authors>MasLazu</Authors>
    <Company>MasLazu</Company>
    <Description>EF Core layer for MasLazu ASP.NET Password Authentication. Contains database context, CQRS implementation, and migrations.</Description>
    <PackageTags>aspnetcore;authentication;efcore;database;cqrs;migrations</PackageTags>
    <RepositoryUrl>https://github.com/MasLazu/MasLazu.AspNet.Authentication.Password</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageProjectUrl>https://github.com/MasLazu/MasLazu.AspNet.Authentication.Password</PackageProjectUrl>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageRequireLicenseAcceptance>false</PackageRequireLicenseAcceptance>
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
  </PropertyGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="MasLazu.AspNet.Framework.EfCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\MasLazu.AspNet.Authentication.Password.Domain\MasLazu.AspNet.Authentication.Password.Domain.csproj" />
  </ItemGroup>

</Project>
