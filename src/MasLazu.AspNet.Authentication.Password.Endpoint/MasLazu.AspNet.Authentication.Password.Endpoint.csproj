<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <!-- NuGet Package Information -->
    <PackageId>MasLazu.AspNet.Authentication.Password.Endpoint</PackageId>
    <Version>1.0.0-preview.1</Version>
    <Authors>MasLazu</Authors>
    <Company>MasLazu</Company>
    <Description>Endpoint layer for MasLazu ASP.NET Password Authentication. Contains REST API endpoints using FastEndpoints framework.</Description>
    <PackageTags>aspnetcore;authentication;api;endpoints;fastendpoints;rest</PackageTags>
    <RepositoryUrl>https://github.com/MasLazu/MasLazu.AspNet.Authentication.Password</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageProjectUrl>https://github.com/MasLazu/MasLazu.AspNet.Authentication.Password</PackageProjectUrl>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageRequireLicenseAcceptance>false</PackageRequireLicenseAcceptance>
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
  </PropertyGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="FastEndpoints" />
    <PackageReference Include="MasLazu.AspNet.Framework.Endpoint" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\MasLazu.AspNet.Authentication.Password.Abstraction\MasLazu.AspNet.Authentication.Password.Abstraction.csproj" />
  </ItemGroup>

</Project>
